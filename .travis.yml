language: elixir

elixir: 1.5.1
otp_release: 19.1

matrix:
  include:
    - elixir: 1.5
      otp_release: 18.0

script:
  mix local.rebar --force # for Elixir 1.3.0 and up
  mix local.hex --force
  mix deps.get
  mix test